.page-login {
  display: flex;
  margin-left: 7.5%;
  border: 1px solid greenyellow;
  height: calc(100% - 1px);
}

.login-form {
  width: 50%;
  border: 5px solid tomato;
  height: 100%;
  box-sizing: border-box;
  display: flex;
  align-items: center;
}
